#!/usr/bin/python

import sys
from underscore import _

def usage():
    print >>sys.stderr, (
        "\n"
        "Usage: _ file.py > _file.py"
        "\n")

if __name__ == '__main__':
    if len(sys.argv) < 2:
        usage()
        sys.exit(1)
    else:
        print _(sys.argv[1])
